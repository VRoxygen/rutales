public enum GameStates {
    Free,
    Ball,
    Leashed
}

public enum DogTypes {
    Beagle,
    Corgi,
    Frenchie,
    Pug
}

public enum DogStates {
    Idle,
    Sitting,
    Walking,
    Running,
    Anticipating
}

public enum DogAnimationStates {
    Idle,
    Sit,
    Lay,
    Aggressive,
    Consume
}