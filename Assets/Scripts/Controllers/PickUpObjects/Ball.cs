//Copyright (c) 2018 - @QuantumCalzone

using UnityEngine;

/*public class Ball : Throwable {

    #region Variables

    private GameManager gameManager;
    private Player player;

    #endregion

    #region Unity Methods

    private void Start() {
        gameManager = GameManager.Instance;
        player = Player.Instance;
    }

    #endregion

    #region Get
    #endregion

    #region Set or Toggle
    #endregion

    #region Methods

    /*public override void Use () {
        base.Use();
        Debug.Log("Imagine a sqeeking noise now", gameObject);
    }

    public override void Interact (HandController hand) {
        base.Interact(hand);
        if (gameManager.GetGameState != GameStates.Ball) gameManager.SetGameState(GameStates.Ball);
    }

    public override void Release(HandController hand) {
        base.Release(hand);
        player.GetDog.OnBallThrow();
    }*/

    //#endregion

//}

